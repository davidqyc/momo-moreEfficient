# Next capture release copy checkpoint

This is a small release reminder, not a new implementation project.

Before a future TestFlight build ships the reading-capture feature:

1. physically compare the Shortcut and Share entry points and choose the primary everyday route;
2. replace the temporary user-facing wording `捕获检查` / `capture review` with the final user term;
3. sync that terminology across the iOS UI, root README, English README, FAQ, `www.jiripple.com/xiaoheiniao/`, and machine-readable public facts;
4. keep unreleased/secondary routes labeled by stage rather than describing them as already available.

The detailed public-copy rules live in [`PUBLIC_COPY_STYLE.md`](PUBLIC_COPY_STYLE.md).

Do not perform this rename now. The final wording should follow the physical-device UX decision rather than precede it.
